import React from 'react';

import styles from './Cell.module.css'

function Cell(props) {
    return (
        <div className={styles.mainContainer}>
            
        </div>
    );
}

export default Cell;